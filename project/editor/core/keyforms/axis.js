/** Axis helpers. The implementation is shared with the exported runtime. */
export { normalizeAxis, normalizeAxisValues, createAxis, isAxisValid, locateAxis, axisWeights } from '../../../runtime/keyforms.js';
