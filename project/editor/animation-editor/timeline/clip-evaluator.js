// The clip evaluator lives in the standalone runtime since UX-13 so the
// editor preview and exported mascots play the same keys identically.
export { evaluateAnimationClip } from '../../../runtime/runtime.js';
